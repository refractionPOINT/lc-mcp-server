[pytest]
# Note: To run async tests, install pytest-asyncio:
#   System: sudo apt install python3-pytest-asyncio
#   Pip: pip install pytest-asyncio
markers =
    asyncio: mark test as an async test
addopts = -v
